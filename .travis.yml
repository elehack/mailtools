language: cpp
install:
  - sudo apt-get update
  - sudo apt-get install -qy libqt4-dev libqtwebkit-dev notmuch-dev tcl-dev libpopt-dev
script:
  - mkdir build
  - cd build && cmake ..
  - make -C build
  - make -C build test
